// import { useSelector } from "react-redux";
import SongBoard from "./SongBoard";

const Home = () => {
  // const songFromRedux = useSelector((state) => {
  //   // state.songMain.song;
  //   // // console.log(songFromRedux);
  // });

  return (
    <>
      <SongBoard />
    </>
  );
};
export default Home;
